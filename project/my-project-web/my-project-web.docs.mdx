---
description: 'The Acme web application.'
labels: ['app', 'react']
---

A React application for the Acme platform.

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use my-project-web 
```

To run the app locally, run the following:

```
bit run my-project-web 
```
